#!/usr/bin/env bash
set -euo pipefail

yamllint --version
shellcheck --version
checkmake --version
ansible-lint --version
ansible-playbook --version
ansible-inventory --version
check-jsonschema --version
bats --version
kcov --version
xmlstarlet --version
jq --version
awk --version
